# 이 프로젝트의 모든 API 키/설정은 이 파일 하나로 관리한다.
# 실제 값은 루트에 .env로 복사해서 채우고, 절대 git에 커밋하지 않는다 (.gitignore에 등록됨).

# --- backend (FastAPI) ---
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
DATABASE_URL=

# --- dashboard / extension (Vite, VITE_ 접두사 필수) ---
VITE_API_BASE_URL=http://localhost:8000
